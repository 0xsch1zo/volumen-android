[package]
name = "volumen-android"
version = "0.1.0"
description = "A Tauri App"
authors = ["you"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "volumen_android_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
reqwest = { version = "0.12.24", features = ["cookies", "json", "rustls-tls", "gzip", "multipart"], default-features = false }
thiserror = "2.0.17"
url = "2.5.7"
env_logger = "0.11.8"
log = "0.4.28"
scraper = "0.24.0"
itertools = "0.14.0"
delegate = "0.13.4"
reqwest-middleware = { version = "0.4.2", features = ["multipart"] }
async-trait = "0.1.89"
tokio = { version = "1.48.0", features = ["rt-multi-thread", "net"] }
